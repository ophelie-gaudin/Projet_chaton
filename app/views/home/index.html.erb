
<div class="jumbotron" >
  <% if current_user %>
    <h1 class="display-4 pt-5 ">Bonjour CatFan</h1>
    <p class="lead">En te souhaitant un agréable voyage sur notre site de vente de photo de chaton de la part de toute l'équipe</p>
  <% else %>
    <h1 class="display-4 pt-5 ">Bienvenue sur ChatonShop</h1>
    <p class="lead">Ici tu pourras trouver tout un tas de petite photo de chaton trop mignonne.</p>
  <% end %>
</div>


<div class="container">
  <div class="d-flex justify-content-around flex-wrap">
 
<%@items.each do |item|%>
  <div class='m-4 col-md-5' >
    <div class="card ">
      <h3 class="card-header" name='title'><%=item.title%></h3>
    <div class="card-body">
    <h5 class="card-title"><%=item.price%> €</h5>

    </div>
    <img src="<%=item.image_url %>" class="d-block user-select-none" width="100%" height="400" aria-label="Placeholder: Image cap" focusable="false" role="img" preserveAspectRatio="xMidYMid slice" viewBox="0 0 318 180" style="font-size:1.125rem;text-anchor:middle" %>
    <%puts item.image_url%>
    <div class="card-body">
      <p class="card-text"><%=item.description%></p>
    </div>
    <div class="card-body">
    <a class="btn btn-outline-info" <%=item_path(item.id)%> role="button">Voir la déscription</a><br /> 
    <%= link_to "Acheter", new_order_path(id: item.id) , class: "btn btn-info"%> 
    </div>
  </div>
  </div>


<%end%>
</div>
</div>
<hr>
