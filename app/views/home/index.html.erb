NAV BAR KEKW

<%@items.each do |item|%>
<div class="card mb-3">
  <h3 class="card-header"><%=item.title%></h3>
   <div class="card-body">
    <h5 class="card-title"><%=item.price%> €</h5>

  </div>
   <img href="<%=item.image_url %>" class="d-block user-select-none" width="100%" height="200" aria-label="Placeholder: Image cap" focusable="false" role="img" preserveAspectRatio="xMidYMid slice" viewBox="0 0 318 180" style="font-size:1.125rem;text-anchor:middle" %>
    <div class="card-body">
    <p class="card-text"><%=item.description%></p>
  </div>
  <div class="card-body">
    <button type="button" class="btn btn-outline-info">Voir la déscription</button><br />
    <button type="button" class="btn btn-info">Acheter</button>
  </div>

<%end%>